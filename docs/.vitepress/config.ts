import { defineConfig } from 'vitepress'

export default defineConfig({
    title: 'JNUN',
    themeConfig: {
        nav: [
            { text: 'hello', link: '/docs/hello' }
        ],
        sidebar: [

        ]
    },
})